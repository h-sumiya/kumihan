name: kumihan
description: "Flutter library for Japanese vertical composition."
version: 0.1.0-dev
repository: "https://github.com/h-sumiya/kumihan"
publish_to: none

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
